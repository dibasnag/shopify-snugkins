<div class="product-template product-template-gallery">
    <div class="container">
        {% if collection %}
        <div class="next_prev-groups">
            {% render 'breadcrumb' %}
            {% render 'next-prev-product' %} 	
        </div>
        
        {% else %}
        {% render 'breadcrumb' %}
        {% endif %}
        
        <div class="halo-product-content">
            <div class="row pro-page">
                {% section 'product-template-gallery' %}
                {% section 'product-recommendations' %}
            </div>
        
            {% if settings.style_product_grid == 'style_product_grid_2' %}
                {% section 'recently-viewed-products-skin-1170' %}
            {% elsif settings.style_product_grid == 'style_product_grid_3' %}
                {% section 'recently-viewed-products-skin-fullwidth' %}
            {% else %}
                {% section 'recently-viewed-products' %}
            {% endif %}
        </div>   
    </div>
</div>